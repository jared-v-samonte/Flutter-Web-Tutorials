/*
import 'package:flutter/material.dart';
import 'package:portfolio/action/action_button.dart';
import 'package:portfolio/views/about/about.dart';

class AboutMonitor extends StatelessWidget 
{
  const AboutMonitor({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) 
  {
    return Row(
      children: <Widget>
      [
        About(),
        const Expanded
        (
          child: Center
          (
            child: ActionButton('Join Course'),
          ),
        )
      ],
    );
  }
}
*/